import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.preethampoonacha.tasktracker',
  appName: 'task-tracker',
  webDir: 'dist/task-tracker/browser'
};

export default config;
